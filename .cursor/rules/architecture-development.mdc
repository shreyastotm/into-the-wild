---
description: "Core architecture and development workflow enforcement"
globs: ["**/*.{ts,tsx,js,jsx,sql,md}", "docker-compose.yml", "Dockerfile*"]
alwaysApply: true
---

# 🏗️ ARCHITECTURE & DEVELOPMENT RULES

## 1.1 Docker-First Development
✅ **REQUIRED:** All database operations must go through Docker containers
❌ **FORBIDDEN:** Direct Supabase cloud database modifications during development
✅ **REQUIRED:** Use `docker-compose up -d postgres backend` for local development
✅ **REQUIRED:** Apply migrations via Docker exec commands, not Supabase CLI

## 1.2 Frontend-Backend Separation
✅ **REQUIRED:** Frontend code must be in `src/` directory only
❌ **FORBIDDEN:** Backend logic in React components
✅ **REQUIRED:** API calls through Supabase client or fetch to backend endpoints
✅ **REQUIRED:** Environment variables prefixed with `VITE_` for frontend

## 1.3 Indian Market Context
✅ **REQUIRED:** Date formats must use DD/MM/YYYY format
✅ **REQUIRED:** Currency display must show ₹ symbol and Indian number formatting
✅ **REQUIRED:** GST calculations must be included where applicable (18% default)
✅ **REQUIRED:** Mobile-first responsive design (320px minimum width)

## 1.4 TypeScript Strict Mode
✅ **REQUIRED:** All TypeScript files must use strict type checking
✅ **REQUIRED:** No `any` types allowed - use proper interfaces
✅ **REQUIRED:** Generic types for reusable components
✅ **REQUIRED:** Proper error type definitions and handling

## 1.5 Deployment Configuration Standards
✅ **REQUIRED:** Use Node.js 22.x (LTS) in production
✅ **REQUIRED:** Install terser as dev dependency for Vite production builds
✅ **REQUIRED:** Use `engines` field in package.json for Node.js version (not vercel.json)
✅ **REQUIRED:** Environment variables must be prefixed with `VITE_` for frontend
✅ **REQUIRED:** All build dependencies must be explicitly installed before deployment

## 1.6 Deployment Authorization & Documentation Standards

### 1.6.1 Explicit Deployment Authorization
✅ **REQUIRED:** All deployments must be explicitly authorized by project lead
✅ **REQUIRED:** Use "DEPLOY" command in commit messages or deployment prompts
✅ **REQUIRED:** Include version number and change summary in deployment authorization
✅ **REQUIRED:** Verify all master documents are current and accurate before deployment
❌ **FORBIDDEN:** Automatic deployments without explicit "DEPLOY" authorization
❌ **FORBIDDEN:** Deployments triggered by CI/CD without manual approval
❌ **FORBIDDEN:** Emergency deployments without documentation updates

### 1.6.2 Pre-Deployment Documentation Requirements
✅ **REQUIRED:** All 5 master documents must be updated and current
✅ **REQUIRED:** Temporary documentation must be consolidated and archived
✅ **REQUIRED:** Run complete documentation quality audit before deployment
✅ **REQUIRED:** Include documentation changelog in deployment summary
✅ **REQUIRED:** Verify all internal documentation links are functional

### 1.6.3 Deployment Quality Gates
✅ **REQUIRED:** Run `npm run quality-check:strict` before deployment
✅ **REQUIRED:** Verify `npm run analyze:accessibility` passes
✅ **REQUIRED:** Confirm `npm run analyze:performance` meets targets
✅ **REQUIRED:** Validate all master documents are accessible and linked correctly
✅ **REQUIRED:** Archive any remaining temporary documentation